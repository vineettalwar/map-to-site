# Claude Integration

Set `ANTHROPIC_API_KEY` in `.dev.vars` or as a Wrangler secret.

Optional override:

```bash
LLM_PROVIDER=anthropic
```

Model: `claude-3-5-sonnet-20241022`

If only OpenAI is configured, the app uses GPT-4o automatically.
